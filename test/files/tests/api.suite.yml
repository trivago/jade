actor: ApiTester
modules:
    enabled:
        - Asserts
        - REST:
            depends: Symfony
            part: Json
        - Symfony:
            var_path: var
            app_path: 'src'
            environment: 'test'
